name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write
  id-token: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          registry-url: "https://registry.npmjs.org"

      - run: bun install --frozen-lockfile

      - name: Verify tag matches package.json version
        run: |
          TAG="${GITHUB_REF_NAME#v}"
          PKG="$(jq -r .version package.json)"
          if [ "$TAG" != "$PKG" ]; then
            echo "tag v$TAG does not match package.json version $PKG" >&2
            exit 1
          fi

      # Before the build, the binaries and npm: decision 0014 makes the release
      # notes the compatibility record for 0.x, so a tag whose version has no
      # CHANGELOG.md section has to fail the job rather than publish a body
      # nobody can read.
      - name: Extract this version's release notes
        run: |
          bun scripts/changelog.ts "${GITHUB_REF_NAME#v}" > "$RUNNER_TEMP/release-notes.md"
          cat "$RUNNER_TEMP/release-notes.md"

      - run: bun run typecheck
      - run: bun run lint
      - run: bun run lint:casts
      - run: bun run format:check
      - run: bun run test

      - name: Build npm distribution
        run: bun run build

      - name: Smoke-test the npm package as installed
        run: |
          node dist/index.js --version
          # Install the real tarball and run the linked bin: npm links
          # node_modules/.bin/gdrive at dist/index.js, and only that path
          # exercises the entry-point check.
          npm pack --pack-destination /tmp
          mkdir -p /tmp/installcheck && cd /tmp/installcheck
          npm install /tmp/ncukondo-gdrive-cli-*.tgz
          ./node_modules/.bin/gdrive --version
          ./node_modules/.bin/gdrive --help | grep -q "Google Drive"

      - name: Compile release binaries
        run: bun run build:release

      - name: Smoke-test the native binary
        run: |
          out/gdrive-linux-x64 --version
          # The tag being released is not published yet, so upgrade can only be
          # smoke-tested for "it runs", not for its exit status.
          out/gdrive-linux-x64 upgrade --dry-run || true

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # The two flags coexist, and the order is not ours to choose: without
          # --notes-start-tag, gh sends the --notes-file content as `body` plus
          # `generate_release_notes: true`, and the REST API "pre-pends" the
          # body to the notes it generates. So the changelog section leads and
          # the commit list follows. --title keeps our own title, which
          # --generate-notes would otherwise replace.
          gh release create "$GITHUB_REF_NAME" out/* install.sh install.ps1 \
            --title "$GITHUB_REF_NAME" \
            --notes-file "$RUNNER_TEMP/release-notes.md" \
            --generate-notes

      - name: Publish to npm (trusted publishing)
        run: |
          # OIDC trusted publishing requires npm >= 11.5.1; Node 22 bundles npm 10
          npm install -g npm@latest
          npm publish --access public --provenance
