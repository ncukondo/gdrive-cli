# Task 0009: Docs commands

Status: todo
Depends on: 0006
Parallel: yes (group C) — alongside 0010

## Goal

`gdrive docs read|create|append|replace|insert` per `decisions/0009`.

## Context

- Relevant decisions: `decisions/0009-docs-commands.md`, `decisions/0007-output-and-errors.md`

## Scope

- `src/lib/docs-api.ts` (Docs v1 wrapper + Docs→markdown/text renderer),
  `src/commands/docs/*` (`index.ts`, `read.ts`, `create.ts`, `append.ts`,
  `replace.ts`, `insert.ts`). Content input reuses `src/lib/input.ts` (from
  task 0002) — do not create a new reader.

## TDD plan

1. **Red** (fake Docs client): `read --as markdown|text`; `create` with
   `--content` + `--parent`; `append` end-of-body; `replace --find/--replace`
   returns count; `insert --index`/`--at`; content input from literal/`@file`/
   stdin; `--format json` shapes per 0009.
2. **Green** — implement wrapper + commands.
3. **Refactor** — extract markdown renderer.

## Acceptance criteria

- [ ] All five subcommands behave and format per 0009
- [ ] `replace` reports occurrences; `insert` honors index vs `--at`
- [ ] `bun run test`, `bun run typecheck` pass; docs updated

## Verification

- `bun run test src/lib/docs-api.test.ts src/commands/docs/*.test.ts`
