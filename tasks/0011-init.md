# Task 0011: `init` command

Status: todo
Depends on: 0003, 0004
Parallel: no

## Goal

`gdrive init` generates `config.toml`, seeding `[[accounts]]` from
authenticated tokens and setting `default_account`, per `decisions/0006`.

## Context

- Relevant decisions: `decisions/0006-configuration.md`

## Scope

- `src/commands/init.ts`; `--local`, `--force`, `--format`, quiet=path.

## TDD plan

1. **Red** — seeds accounts from `accounts/*.json`; sets default to sole/first;
   refuses existing file without `--force`; `--local` writes `./gdrive-cli.toml`;
   text/json/quiet outputs.
2. **Green** — implement over `lib/config.ts`.
3. **Refactor** — reuse config writer.

## Acceptance criteria

- [ ] Generates a valid config; idempotent guard via `--force`
- [ ] Output shows path + accounts + default (text/json), quiet=path
- [ ] `bun run test`, `bun run typecheck` pass; docs updated

## Verification

- `bun run test src/commands/init.test.ts`
