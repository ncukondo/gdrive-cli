{
  "name": "@ncukondo/gdrive-cli",
  "version": "0.8.0",
  "description": "CLI for Google Drive, Docs, Sheets, and Forms with multi-account switching, designed for humans and AI agents",
  "keywords": [
    "google-drive",
    "google-docs",
    "google-sheets",
    "cli",
    "gdrive",
    "drive",
    "sheets",
    "docs",
    "forms",
    "oauth",
    "multi-account"
  ],
  "license": "MIT",
  "author": "ncukondo",
  "homepage": "https://github.com/ncukondo/gdrive-cli#readme",
  "bugs": {
    "url": "https://github.com/ncukondo/gdrive-cli/issues"
  },
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "bin": {
    "gdrive": "dist/index.js"
  },
  "files": [
    "dist",
    "CHANGELOG.md"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ncukondo/gdrive-cli.git"
  },
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target node --packages external",
    "build:bin": "bun build src/index.ts --compile --outfile gdrive",
    "build:release": "bun scripts/build-release.ts",
    "test": "vitest run --exclude 'tests/e2e/**'",
    "test:watch": "vitest --exclude 'tests/e2e/**'",
    "test:unit": "vitest run src scripts",
    "test:integration": "vitest run tests/integration",
    "test:e2e": "vitest run tests/e2e",
    "lint": "oxlint src tests scripts",
    "lint:casts": "bun scripts/lint-casts.ts",
    "changelog": "bun scripts/changelog.ts",
    "format": "oxfmt src tests scripts",
    "format:check": "oxfmt --check src tests scripts",
    "typecheck": "tsc --noEmit",
    "prepare": "husky || true",
    "prepublishOnly": "bun run build"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "googleapis": "^173.0.0",
    "smol-toml": "^1.0.0",
    "yaml": "^2.9.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0",
    "oxlint": "latest",
    "oxfmt": "latest",
    "husky": "^9.1.7"
  }
}
